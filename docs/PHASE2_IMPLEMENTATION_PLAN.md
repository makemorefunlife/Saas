# Phase 2: 상품 기능 구현 계획

## 🎯 구현 목표

홈 페이지에서 상품 목록 페이지로의 진입 동선을 구축하고, Grid 레이아웃으로 상품을 표시하는 상품 목록 페이지를 구현합니다.

---

## 📋 구현 범위

### 1. 홈 페이지 개선
**목표**: 상품 진입 동선 추가

#### 현재 상태
- My Korean Name 서비스 랜딩 페이지
- "Start Your Journey" CTA 버튼

#### 구현 내용
- [ ] 상품 진입 섹션 추가
  - 인기 상품 미리보기 (최대 4개)
  - "모든 상품 보기" 버튼
  - 카테고리별 빠른 진입 버튼 (선택사항)

#### 디자인 요구사항
- Modern Seoul 테마 유지 (Black 배경, Neon Pink/Purple)
- Spacing-First 정책 준수 (gap 사용)
- 반응형 디자인 (모바일 우선)

---

### 2. 상품 목록 페이지 (`/products`)
**목표**: Grid 레이아웃으로 상품 목록 표시

#### 페이지 구조
```
/products
├── 헤더
│   ├── 페이지 제목
│   └── 상품 개수 표시
├── 필터/정렬 바 (Phase 2-2에서 구현)
│   ├── 카테고리 필터
│   └── 정렬 옵션
└── 상품 그리드
    └── ProductCard 컴포넌트들
```

#### Grid 레이아웃 스펙
- **모바일**: 1열 (grid-cols-1)
- **태블릿**: 2열 (md:grid-cols-2)
- **데스크톱**: 3열 (lg:grid-cols-3)
- **큰 화면**: 4열 (xl:grid-cols-4)
- **간격**: `gap-4 md:gap-6`

#### 상품 카드 컴포넌트 (`ProductCard`)
**표시 정보**:
- 상품 이미지 (placeholder 또는 실제 이미지)
- 상품명
- 가격 (원화 포맷)
- 카테고리 태그
- 재고 상태 (선택사항)
- "장바구니 추가" 버튼 (Phase 3에서 기능 구현)

**디자인**:
- 카드 스타일: `bg-white/5 border border-white/10 rounded-lg`
- 호버 효과: `hover:border-pink-400/50 hover:bg-white/10`
- 이미지 비율: `aspect-square` (1:1)

---

## 🏗 기술 스펙

### 데이터 페칭
- **라이브러리**: React Query (`@tanstack/react-query`)
- **서버 컴포넌트**: 상품 목록은 Server Component로 구현
- **캐싱**: React Query 캐싱 활용

### 타입 정의
```typescript
// types/product.ts
export interface Product {
  id: string;
  name: string;
  description: string | null;
  price: number;
  category: string | null;
  stock_quantity: number;
  is_active: boolean;
  created_at: string;
  updated_at: string;
}
```

### API 함수
```typescript
// lib/supabase/products.ts
export async function getProducts(params?: {
  category?: string;
  limit?: number;
  offset?: number;
}): Promise<Product[]>
```

---

## 📁 파일 구조

```
app/
├── page.tsx                    # 홈 페이지 (상품 진입 섹션 추가)
└── products/
    └── page.tsx               # 상품 목록 페이지

components/
├── ProductCard.tsx             # 상품 카드 컴포넌트
└── ProductGrid.tsx             # 상품 그리드 레이아웃 (선택사항)

lib/
└── supabase/
    └── products.ts             # 상품 데이터 페칭 함수

types/
└── product.ts                  # Product 타입 정의
```

---

## ✅ 구현 체크리스트

### Step 1: 타입 및 유틸리티
- [ ] `types/product.ts` 생성 (Product 인터페이스)
- [ ] `lib/supabase/products.ts` 생성 (getProducts 함수)
- [ ] 가격 포맷팅 유틸리티 함수 (`lib/utils/formatPrice.ts`)

### Step 2: 컴포넌트 구현
- [ ] `components/ProductCard.tsx` 생성
  - [ ] 이미지 표시 (Next.js Image 컴포넌트)
  - [ ] 상품명, 가격, 카테고리 표시
  - [ ] 호버 효과
  - [ ] 반응형 디자인
- [ ] `components/ProductGrid.tsx` 생성 (선택사항, 또는 인라인으로 구현)

### Step 3: 페이지 구현
- [ ] `app/products/page.tsx` 생성
  - [ ] Server Component로 상품 데이터 페칭
  - [ ] Grid 레이아웃 적용
  - [ ] 로딩 상태 처리
  - [ ] 빈 상태 처리 (상품이 없을 때)
- [ ] `app/page.tsx` 수정
  - [ ] 상품 진입 섹션 추가
  - [ ] 인기 상품 미리보기 (최대 4개)

### Step 4: 스타일링
- [ ] Spacing-First 정책 준수 (gap 사용)
- [ ] Modern Seoul 테마 적용
- [ ] 반응형 디자인 검증
- [ ] 다크모드 대응 (이미 dark 모드)

### Step 5: 테스트 및 검증
- [ ] 상품 목록이 정상적으로 표시되는지 확인
- [ ] Grid 레이아웃이 반응형으로 작동하는지 확인
- [ ] 이미지 로딩 및 에러 처리 확인
- [ ] 빈 상태 UI 확인

---

## 🎨 디자인 가이드

### 색상
- 배경: `bg-black` (oklch(0.05 0 0))
- 카드 배경: `bg-white/5`
- 카드 테두리: `border-white/10`
- 호버: `hover:border-pink-400/50`
- 텍스트: `text-white`, `text-white/70`, `text-white/60`

### 타이포그래피
- 페이지 제목: `text-3xl md:text-4xl font-bold`
- 상품명: `text-lg font-semibold`
- 가격: `text-xl font-bold text-pink-400`
- 카테고리: `text-sm text-white/60`

### 간격
- 페이지 패딩: `p-4 md:p-6 lg:p-8`
- 그리드 간격: `gap-4 md:gap-6`
- 카드 내부: `p-4 md:p-6`

---

## 📝 다음 단계 (Phase 2-2)

현재 구현에서는 다음 기능은 제외:
- 페이지네이션 (Phase 2-2)
- 정렬 기능 (Phase 2-2)
- 카테고리 필터 (Phase 2-2)
- 검색 기능 (Phase 2-2)

이 기능들은 상품 목록 페이지가 기본적으로 작동한 후 추가합니다.

---

## 🔗 관련 파일

- [ggultip.mdc 규칙](../.cursor/rules/ggultip.mdc)
- [Next.js Convention](../.cursor/rules/web/nextjs-convention.mdc)
- [TODO.md](./TODO.md)

